import { Component } from '@angular/core';

@Component({
  selector    : 'navigator',
  templateUrl : './navigator.template.html',
  providers   : [ ]
})

export class NavComponent {
  constructor(){}
};
